# Close the Loop — 2026-W37

Completion receipt: **COMPLETE**, first eligible review on 2026-09-13.
Source schedule: `AGENT-TEAM/SCHEDULE.md`, Friday 19:40 America/Chicago.
Next due: **2026-09-18T19:40:00-05:00** (2026-W38).

This is the first synthesis, caught up after the week's Friday slot. The
product and team began September 12, after that slot; no September 11
feedback history or missed member response is inferred. A later run
checks this receipt before repeating W37.

## Feedback and judgment evidence

The complete `feedback#queue` partition contained **0 items**, measured
read-only before editing. After the repair deployed, the required
`AWS_PROFILE=jamie node scripts/feedback.mjs list` returned "nothing
waiting". Remaining backlog: **0**. Unanswered: **0**. Oldest unanswered
age: **not applicable**. One-day target breaches: **0**.

There are no feedback themes, member judgment disputes or leader requests
in this sample. No engine-versus-record attribution can be made from an
empty queue. The historical second-push comparisons in `docs/NOTES.md`
are development evidence, not member complaints. No reply was delivered
or repeated, and no feedback, decision, hold, note or grant was manufactured.

The feedback email subscription is confirmed. No organic feedback item
exists yet to establish end-to-end notification or reply acceptance.

## Ranked improvements

1. **Restore feedback reads — shipped `1ceee42`.** The first queue command
   failed because the DynamoDB query used an empty sort-key value. The
   shared ledger now queries the whole partition without that predicate;
   typed clan lists retain their prefixes and pagination remains intact.
   This restores feedback lists, unseen counts and the maintainer queue;
   clan cleanup uses the same corrected path but was tested offline only.
   The regression failed before the repair and passes now.
2. **Keep deployment verification read-only — shipped `1ceee42`.** Smoke
   previously created a pending-login item on GET `/auth/login`. It now
   checks the public and signed-out surfaces without entering login. The
   subprocess regression caught the old write-producing request. The
   offline handler tests retain redirect, scope, PKCE and cookie coverage.
   A new human sign-in is not claimed from this smoke.
3. **Remove completed setup work from the docs — shipped `1ceee42`.** The
   production hostname works and CI really deploys, with both required
   secret names present (values unread). The first-push instructions
   incorrectly left DNS and credential setup waiting on Jamie. The guide
   also now describes the existing ledger retention and shipped engine.
4. **Consolidate Elder guidance — existing Jamie decision remains open.**
   poapkings.com's Members and FAQ still carry their own prose and have
   no link to this product's policy page. Smallest proposal: approve
   replacing that prose with the existing public how-Elder-works link.
   The public page's clan-name proposal is separate: adding a stored name
   or changing the public document shape requires Jamie. Neither was
   changed here.
5. **Check the upstream participation latency on the next natural read.**
   `docs/NOTES.md` records the prior measured 12.8-second average for
   `clans_participation`. No `slow_elixir_call` event was returned from
   this run's log read, so that older measurement is not asserted as
   current performance or a confirmed continuing failure. A new natural
   trace can establish whether Elixir still needs a fact-serving fix.

Publishing the design package still needs Jamie's optional npm org; the
pinned dependency works. Elixir's published contract remains 1.9.0, the
version this product's participation read requires. No OAuth scope changed.

## Shipping and acceptance receipt

Repair: `1ceee42fef5a29271eda8f55bd2c3c6d9e141bf9`.
`npm run verify`: **passed, 182 tests**. CI
[validate](https://github.com/jthingelstad/clan.poapkings.com/actions/runs/34758014677)
and [deploy](https://github.com/jthingelstad/clan.poapkings.com/actions/runs/34758044948)
both completed successfully for that commit, including deployment smoke.

The live stack is `UPDATE_COMPLETE` on API artifact
`code/api/1cb3c22e4da543ac.zip`. The uploaded bundle equals the local
bundle byte-for-byte, and the ZIP's SHA-256 equals the active Lambda's
`CodeSha256`. Read-only live acceptance passed the queue read, smoke,
and public Elder policy (version 0); the awards document correctly answers
`404 not_published` with its five-minute public cache header.

Next natural acceptance: the first organic feedback item must appear in
the queue and notification path; its eventual authorized reply must read
back on the member's page. Until then, the empty queue is confirmed and
the member-facing notification/reply path has insufficient natural sample.

The API error, 5xx and slow-request alarms are OK. The billing alarm is
account-wide `AWS/Billing EstimatedCharges`, not product spend: a $19.73
datapoint crossed $10. Run Elixir Clan owns its operational interpretation.
No alarm, policy, award configuration or member standing was changed.
